#include<stdio.h>
/**
 * main - Entry Point
 *
 * Description: Print
 * Return: Always 0 if Success.
 */
int main(void)
{
	int num;

	for (num = 1; num <= 10; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
