#include<stdio.h>
/**
 * main - Entry Point
 *
 * Description: Print
 * Return: Always 0 if Success.
 */
int main(void)
{
	int num;

	for (num = 0; num <= 9; num++)
	{
		printf("%d", num);
	}
	printf("\n");
	return (0);
}
