#include<stdio.h>
/**
 * main - Entry Point
 *
 * Description: Print the alphabet in lowercase
 * Return: Always 0 if Success.
 */
int main(void)
{
	int a;
	for ( a = 'a'; a <= 'z'; a++)
{
putchar(a);
}
putchar('\n');
return (0);
}
